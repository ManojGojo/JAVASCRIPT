const obj={
    Name: "Manoj",
    age: 22,
    course: "Java Script",
    info:{
        city:"Patan",
        District:"Satara"
    }
}

// const entries=Object.entries(obj);
// console.log(entries);

const keys=Object.keys(obj);
const values=Object.values(obj);


console.log(keys);
console.log(values);